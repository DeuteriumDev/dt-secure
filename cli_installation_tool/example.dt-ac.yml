version: 0.1
pg_url: ${POSTGRES_URL}
tables:
  - folders
  - constants
  - hives

api_key: ${DT_API_KEY}
dt_url: ${DT_URL}

# schema to store local db configs and info on, defaults to public schema
schema: public

user_source:
  # internal users configuration
  table: users

  # external users api configuration
  # api:
  #   url: ${USERS_API_URL}
  #   headers:
  #     api-key: ${USERS_API_KEY}
  #   paging:
  #     type: limit-offset
